# ============================================================
# Core Agent & Orchestration
# ============================================================
agno>=1.0.0
agno[ollama]>=1.0.0

# MCP runtime (FastMCP pulls starlette/uvicorn/httpx-sse/etc. transitively)
mcp>=1.25,<2

# ============================================================
# Settings / Config
# ============================================================
python-dotenv>=1.0.1
pydantic>=2.8,<3
pyyaml>=6.0.2

# ============================================================
# Compatibility pins for MCP stdio stability
# ============================================================
# sse-starlette requires anyio>=4.7.0; cap to avoid known regressions in newer anyio.
anyio>=4.7,<4.12
httpx>=0.27,<0.29

# ============================================================
# HTTP / APIs
# ============================================================
requests>=2.32

# ============================================================
# Data & Analytics
# ============================================================
pandas>=2.2
numpy>=2.0

# ============================================================
# GTFS-Realtime (CRITICAL)
# ============================================================
gtfs-realtime-bindings>=1.0.0
protobuf>=4.25,<6

# ============================================================
# Serialization & CLI tooling
# ============================================================
orjson>=3.10
tqdm>=4.66

# ============================================================
# Optional: SQLite persistence for agent memory
# ============================================================
pysqlite3-binary>=0.5.4 ; platform_system != "Windows"

# Optional CLI/logging (only if your code imports them)
typer>=0.12
click>=8.1
loguru>=0.7
nest-asyncio>=1.6
asyncio-extras>=1.3
